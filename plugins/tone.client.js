// Client-only plugin to initialize Tone.js
export default defineNuxtPlugin(() => {
  // This plugin only runs on the client side
  // Tone.js will be available globally through dynamic imports in composables
})

